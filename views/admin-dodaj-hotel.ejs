<!DOCTYPE html>
<html lang="pl">
   <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link rel="stylesheet" type="text/css" href="/css/admin-dodaj-hotel.css" />
      <link rel="stylesheet" type="text/css" href="/css/admin-header.css" />
      <title>Dodaj hotel</title>
   </head>
   <body>
      <%- include('partials/header-admin') -%>
      <main>
         <form method="POST">
            <h2>Dodaj wpis do hoteli:</h2>
               <div class="error">
                  <ul>
                  <% if (typeof errors !== 'undefined'){ %>
                     <% for (const key in errors) {%>
                        <li><%= errors[key].message%></li>
                     <% } %>
                  <%}%>
                  <% if (typeof duplicate !== 'undefined'){ %>
                     <li>Nazwa: "<%= ans.name %>" już istnieje!</li>
                  <%}%>

                  </ul>
               </div>
            
            <div class="form-group">
               <label for="name">Nazwa:</label>
               <input type="text" id="name" name="name" placeholder="Nazwa hotelu" value='<%= ans.name %>' >
            </div>
            <div class="form-group">
               <label for="place">Miejscowość:</label>
               <input type="text" id="place" name="place" placeholder="Nazwa miejscowości" value='<%= ans.place %>' >
            </div>
            <div class="form-group">
               <label for="desc">Opis:</label>
               <textarea id="desc" name="desc" rows="4" placeholder="Opis"  ><%= ans?.desc %></textarea>
            </div>
            <div class="form-group">
               <label for="price">Cena:</label>
               <input type="number" id="price" name="price" placeholder="Cena hotelu" value='<%= ans?.price %>'>
            </div>
            <div class="form-group">
               <label for="faci">Najlepsze udogodnienia:</label><br>
               <textarea name="faci" cols="50" rows="10" placeholder="Udogodnienia oddziel przecinkiem"><%= ans?.faci %></textarea><br>
            </div>
            <div class="form-group">
               <label for="rate">Ocena</label>
               <input type="number" id="rate" name="rate" placeholder="Ocena" value='<%= ans?.rate %>' >
            </div>
            <div class="form-group">
               <label for="booking">Max długość rezerwacji:</label>
               <input type="number" id="booking" name="booking" placeholder="Podaj ilość dni" value='<%= ans?.booking %>'>
            </div>
            <div class="form-group">
               <label for="map">Link do mapy</label>
               <input type="text" id="map" name="map" placeholder="Podaj link do mapy" value='<%= ans?.map %>'>
            </div>
            <div class="form-group">
               <label for="mainImg">Zdjęcie główne</label>
               <input type="text" id="mainImg" name="mainImg" placeholder="Podaj link do głównej grafiki" value='<%= ans?.mainImg %>' >
            </div>
            <div class="form-group">
               <label>Zdjęcia do galerii:</label>
               <input type="text" name="subImg" placeholder="Link do sub grafiki 1" value='<%= ans?.subImg[0] %>'>
               <input type="text" name="subImg" placeholder="Link do sub grafiki 2" value='<%= ans?.subImg[1] %>'>
               <input type="text" name="subImg" placeholder="Link do sub grafiki 3" value='<%= ans?.subImg[2] %>'>
               <input type="text" name="subImg" placeholder="Link do sub grafiki 4" value='<%= ans?.subImg[3] %>'>
               <input type="text" name="subImg" placeholder="Link do sub grafiki 5" value='<%= ans?.subImg[4] %>'>
            </div>
            <div class="buttons">
               <a href="/admin/hotele"><button type="button" class="btn btn-back">Cofnij</button></a>
               <button type="submit" class="btn">Zapisz</button>
            </div>
         </form>
      </main>
   </body>
</html>
